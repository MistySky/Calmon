# CalMon 实施规则

## 授权与依据

- 本仓库为开发交接规格；只有用户明确授权开始实现后，才创建工程或编写应用代码。
- 实施前完整阅读 docs/SPEC.md、docs/UI_SPEC.md、docs/MONITORING.md、docs/ACCEPTANCE.md。
- 本轮 UI/功能收敛先阅读 docs/UI_REFINEMENT.md；它覆盖旧文档中内存组成展示、手选节日来源及本轮明确调整的视觉要求，其他约束继续有效。仅整理文档时不得实施应用代码。
- 最新界面补充先读 docs/UI_REVIEW_FOLLOWUP.md；其普通日历降级、无内置节假日/调休兜底、单行详情摘要等要求优先于 UI_REFINEMENT.md 的冲突内容。
- 以这些文档作为统一依据，不依赖聊天、旧原型或自行推断功能。用户最新明确指令优先。
- 允许依据规格自主选择可逆的实现细节。不得降低准确性标准、放宽性能预算、隐瞒数据缺失或引入范围外功能。
- API 或数据不能满足需求时提交技术证据和具体缺口，不把占位结果当作已完成。

## 必须遵守

- macOS 27、Swift、SwiftUI、AppKit；使用支持该系统的稳定 SDK，记录实际工具链。零第三方依赖、零数据库、公开 API。
- 一个 App target；Calendar 与 Monitoring 分目录、分状态、分采样生命周期。不要拆成多个 framework 或包。
- 两个独立 NSStatusItem；独立左击路由和弹窗锚点，右击菜单一致。不得合并成一个大按钮。
- SwiftUI 负责内容，AppKit 负责状态栏和必要窗口集成；优先 Observation。共享偏好不承载全部业务状态。
- UI 参数集中于轻量常量文件；系统管理的控件使用原生样式，不自绘玻璃、开关或菜单。
- 当前数值圆环、占用条属于范围（内存组成展示已按 UI_REFINEMENT 移除）；历史曲线、记录监控历史、持续动画不属于范围。
- 一个 3 秒监控调度源；逐应用数据只在监控面板可见时读取；磁盘容量按 30 秒节奏复用同一调度源。
- CPU/内存的菜单栏、圆环、概况必须来自同一快照，不在视图中重复计算或独立请求。
- 不以总内存减空闲页代替活动监视器口径；不以进程常驻内存 RSS 冒充 footprint。
- 班/休来源明确，不能按周末或节日名称猜测。数据覆盖不足显示未知。
- 不通过 shell 命令、私有 API、管理员 helper、进程注入或屏幕读取实现产品监控。
- 只持久化设置；节气年度数据可作为版本化静态资源（不含节假日/调休），不保存运行监控历史。
- 不擅自引入个人日程、事件编辑、通知、结束进程、温度/GPU/网络/磁盘读写速度、云服务或自动更新。
- 不改用户正式图标源文件；复制需要的 appiconset 到应用资源目录。

## 交付

- 完成有意义的计算/日期/状态测试和 Release 验证，按照 ACCEPTANCE.md 提交结果。
- 文档中的性能上限是验收预算，不是实测声明；未测项必须标记“未执行”。
- 可复现构建步骤、最终监控字段映射、数据覆盖范围、截图与性能证据缺一不可。
- 新增验收证据放 docs/validation/，不恢复讨论记录或原型图库，不把 docs/ 打包进 App。

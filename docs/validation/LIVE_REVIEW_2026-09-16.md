# 运行中 App 检查（2026-09-16）

## 范围与结论

不修改应用代码、不构建、不安装、不更改设置或日历授权。本次已读到运行实例的设置界面；菜单栏及弹窗交互受界面工具限制，尚未完成，不能据此宣布整体验收通过。

## 实机已观察

| 项目 | 结果 |
| --- | --- |
| Release 设置 | “系统日历权限 已授权”；周起始日为周日，单选组已靠右排列 |
| Debug 设置 | 仍显示“获取权限”；未点击请求或授予权限 |
| 运行实例 | PID 60544 为 build/Build/Products/Release/CalMon.app；PID 43739 为 Debug/CalMon.app |
| 实例来源限制 | 首次按名称连接工具超时后才获取进程清单；工具可能唤起 Debug，不能断言双实例在检查前就存在，不能把它直接记为产品缺陷 |
| 当前磁盘产物 | 两个 App 均含 com.apple.security.personal-information.calendars=true；上一轮临时构建缺 entitlement 的结论不能套用到这些产物 |
| Release 签名 | ad-hoc、hardened runtime、无 TeamIdentifier；不能凭当前“已授权”证明未来覆盖升级的授权继承 |

进程资源单次快照：Release CPU 0.0%、RSS 138448 KiB；Debug CPU 0.1%、RSS 107904 KiB。RSS 不是 physical footprint，瞬时 CPU 不是规定窗口均值；不作为性能预算通过或失败依据。

## 当前源码补充（不是已复现的运行现象）

已检查到的新修订包括：权限错误记录、无来源显示普通日历、周起始控件 fixedSize、单行日期摘要、详情统一 infoRow、卡片共用背景、应用行 ID 与图标缓存使用 rootPath、来源变化重新发现。不得照抄旧报告把这些全部当成未修复。

仍存在来源识别风险：HolidayProvider.holidayCalendarCandidate(_:) 在标题同时含中国与节日关键词时直接返回 true，跳过只读/订阅元数据核验。名为“中国节日计划”的个人可编辑日历也会命中。应补严格来源边界和反例测试；本次没有创建此类日历或读取个人事件来复现。

## 未完成项及阻碍

- 工具能返回设置窗口截图与可访问性树，但收起窗口、键盘访问菜单栏的尝试没有得到预期界面变化，未获得菜单栏状态项或两个弹窗的实时画面。
- 因此菜单栏真实间距、日历与监控毛玻璃同条件对照、当前日期详情排版、应用聚合实际数值、Esc/外部点击关闭均未验证。
- 已请求用户手动打开监控弹窗以继续读取；没有关闭进程、重启 App、重置 TCC 或变更登录项。
- 日历首次授权、拒绝后恢复、撤销授权、覆盖安装后的授权继承仍未执行。
